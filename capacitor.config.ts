import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.formstr.drive",
  appName: "Drive by Form*(Experimental)",
  webDir: "dist",
};

export default config;
